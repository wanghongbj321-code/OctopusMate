# 角度打分表（步骤 01-05 · angleScores）

> 用途：按顾问确认的 `scoring_config.anchors.{V/I/T/A/L}` 逐角度打分并记录判断与证据。
> 对应契约字段：`angleScores`（必填，VITAL 为 22 项）；打分规则唯一事实源 = state.json.scoring_config，锚点提示须引用该配置而非默认锚点文本（`frameworks/anchors.md`）。

## 维度：{V / I / T / A / L} · {维度名}

| 角度 | 打分参照锚点（scoring_config，顾问确认） | 打分 | 核心判断 | 证据编号 | 阻断标记 |
|---|---|---|---|---|---|
| V1 战略承接 | 1分：… / 3分：… / 5分：… |  | 战略承接是否清晰 | E-xx | ☐ ≤阈值 |
| V2 业务边界 |  |  |  |  | ☐ |
| … |  |  |  |  | ☐ |

> 打分规则：1-5 分（0.5 步进）；维度分 = 本维各角度算术平均（保留一位小数）；超出职责范围的角度不打分（不计入）。
> 每角度必须有证据支撑（证据可复核红线）；缺双来源的标记"待补强"，不以低分代替未知状态。

## 维度汇总

- V 维均分：`(V1+V2+…)/n = __.__` 分
- 低分角度（≤ 阻断阈值）：____
